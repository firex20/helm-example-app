apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-example-app.fullname" . }}
  labels:
    {{- include "helm-example-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-example-app.selectorLabels" . | nindent 4 }}
